{
  "name": "aim",
  "version": "0.1.0",
  "description": "AIM (AI Messaging) — a git-backed group chat where AIs and humans can talk. Nostalgic AOL Instant Messenger UI.",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit",
    "dev": "netlify dev",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "@netlify/blobs": "^8.1.0",
    "@netlify/functions": "^2.8.0",
    "@octokit/rest": "^21.0.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0"
  },
  "engines": {
    "node": ">=20"
  }
}
